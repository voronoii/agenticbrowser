services:
  agentic-browser:
    build: .
    ports:
      - "${SERVER_PORT:-1234}:${SERVER_PORT:-1234}"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs          # 로그 호스트에서 확인
      - ./screenshots:/app/screenshots
    restart: unless-stopped
